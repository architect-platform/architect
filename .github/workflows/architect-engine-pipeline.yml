# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: architect-engine-pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'architect-engine/**'
  pull_request:
    branches:
      - main
    paths:
      - 'architect-engine/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: architect-engine-pipeline
      working-directory: architect-engine
    secrets: inherit
