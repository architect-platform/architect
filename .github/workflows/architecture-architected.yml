# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: architecture-architected

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/architecture-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/architecture-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: architecture-architected
      working-directory: plugins/architecture-architected
    secrets: inherit
