# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: gradle-architected-pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/gradle-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/gradle-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: gradle-architected-pipeline
      working-directory: plugins/gradle-architected
    secrets: inherit
