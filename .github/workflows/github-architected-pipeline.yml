# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: github-architected-pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/github-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/github-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: github-architected-pipeline
      working-directory: plugins/github-architected
    secrets: inherit
