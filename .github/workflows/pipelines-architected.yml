# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: pipelines-architected

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/pipelines-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/pipelines-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: pipelines-architected
      working-directory: plugins/pipelines-architected
    secrets: inherit
