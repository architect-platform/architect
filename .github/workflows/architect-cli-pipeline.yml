# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: architect-cli-pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'architect-cli/**'
  pull_request:
    branches:
      - main
    paths:
      - 'architect-cli/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: architect-cli-pipeline
      working-directory: architect-cli
    secrets: inherit
