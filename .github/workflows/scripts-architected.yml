# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: scripts-architected

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/scripts-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/scripts-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: scripts-architected
      working-directory: plugins/scripts-architected
    secrets: inherit
