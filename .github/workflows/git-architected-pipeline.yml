# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: git-architected-pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/git-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/git-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: git-architected-pipeline
      working-directory: plugins/git-architected
    secrets: inherit
