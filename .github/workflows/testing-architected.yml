# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: testing-architected

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/testing-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/testing-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: testing-architected
      working-directory: plugins/testing-architected
    secrets: inherit
