# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: docs-architected-pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/docs-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/docs-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: docs-architected-pipeline
      working-directory: plugins/docs-architected
    secrets: inherit
