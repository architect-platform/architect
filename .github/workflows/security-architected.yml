# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: security-architected

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/security-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/security-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: security-architected
      working-directory: plugins/security-architected
    secrets: inherit
