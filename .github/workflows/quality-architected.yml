# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: quality-architected

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/quality-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/quality-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: quality-architected
      working-directory: plugins/quality-architected
    secrets: inherit
