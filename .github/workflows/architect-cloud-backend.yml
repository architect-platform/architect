# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: architect-cloud-backend

on:
  push:
    branches:
      - main
    paths:
      - 'architect-cloud/backend**'
  pull_request:
    branches:
      - main
    paths:
      - 'architect-cloud/backend**'

jobs:
  build:
    name: architect-cloud-backend
    runs-on: ubuntu-latest
    concurrency:
      group: build-${{ github.workflow }}-${{ github.event.number || github.ref }}
      cancel-in-progress: true
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Set up Architect
        run: |
          curl -sSL https://raw.githubusercontent.com/architect-platform/architect/main/architect-cli/.installers/bash-ci | bash

      - name: Set up Architect Engine
        run: |
          curl -sSL https://raw.githubusercontent.com/architect-platform/architect/main/architect-engine/.installers/bash-ci | bash
          architect engine start

      - name: Init
        run: architect init --plain
        working-directory: architect-cloud/backend

      - name: Verify
        run: architect verify --plain
        working-directory: architect-cloud/backend

      - name: Build
        run: architect build --plain
        working-directory: architect-cloud/backend

      - name: Test
        run: architect test --plain
        working-directory: architect-cloud/backend