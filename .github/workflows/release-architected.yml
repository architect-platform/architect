# This file is generated by Architect. Do not edit it directly.
# It will be overwritten by the next Architect run.
name: release-architected

on:
  push:
    branches:
      - main
    paths:
      - 'plugins/release-architected/**'
  pull_request:
    branches:
      - main
    paths:
      - 'plugins/release-architected/**'

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-kotlin-pipeline.yml
    with:
      module-name: release-architected
      working-directory: plugins/release-architected
    secrets: inherit
